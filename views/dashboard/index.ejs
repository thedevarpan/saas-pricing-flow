<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - NextAi</title>
    <link rel="icon" href="/assets/img/favicon.png" type="image/png">
    <link rel="stylesheet" href="/assets/css/bootstrap.css">
    <link rel="stylesheet" href="/assets/css/bootstrap-icons.css">
    <link rel="stylesheet" href="/assets/css/style.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #f5f6fa;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: #2d3436;
            overflow-x: hidden;
        }

        /* Sidebar Styles */
        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            width: 260px;
            height: 100vh;
            background: #fff;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.05);
            padding: 2rem 0;
            z-index: 1000;
            transition: transform 0.3s ease;
        }

        .sidebar-logo {
            padding: 0 1.5rem 2rem 1.5rem;
            border-bottom: 1px solid #e9ecef;
            margin-bottom: 2rem;
        }

        .sidebar-logo h2 {
            font-size: 1.5rem;
            font-weight: 700;
            color: #2d3436;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .sidebar-logo i {
            font-size: 1.75rem;
            color: #6c5ce7;
        }

        .sidebar-nav {
            list-style: none;
            padding: 0;
        }

        .sidebar-nav li {
            margin: 0;
        }

        .sidebar-nav a {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 0.875rem 1.5rem;
            color: #636e72;
            text-decoration: none;
            transition: all 0.3s ease;
            font-weight: 500;
            font-size: 0.95rem;
        }

        .sidebar-nav a:hover,
        .sidebar-nav a.active {
            background: linear-gradient(90deg, rgba(108, 92, 231, 0.1) 0%, transparent 100%);
            color: #6c5ce7;
            border-left: 3px solid #6c5ce7;
        }

        .sidebar-nav a i {
            font-size: 1.25rem;
            width: 24px;
        }

        .sidebar-footer {
            position: absolute;
            bottom: 2rem;
            left: 0;
            right: 0;
            padding: 0 1.5rem;
        }

        .upgrade-card {
            background: linear-gradient(135deg, #6c5ce7 0%, #a29bfe 100%);
            border-radius: 15px;
            padding: 1.5rem;
            color: #fff;
            text-align: center;
        }

        .upgrade-card h4 {
            font-size: 0.95rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .upgrade-card p {
            font-size: 0.8rem;
            opacity: 0.9;
            margin-bottom: 1rem;
        }

        .upgrade-card .btn {
            background: #fff;
            color: #6c5ce7;
            border: none;
            padding: 0.5rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            font-size: 0.875rem;
        }

        /* Main Content */
        .main-content {
            margin-left: 260px;
            padding: 2rem 2.5rem;
            min-height: 100vh;
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .header-left h1 {
            font-size: 2rem;
            font-weight: 700;
            color: #2d3436;
            margin-bottom: 0.25rem;
        }

        .header-left p {
            color: #636e72;
            font-size: 0.95rem;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .date-badge {
            background: #fff;
            padding: 0.5rem 1rem;
            border-radius: 10px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: #2d3436;
            font-weight: 500;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
            font-size: 0.9rem;
        }

        /* User Profile */
        .user-profile {
            background: #fff;
            padding: 1.5rem;
            border-radius: 15px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
            text-align: center;
            min-width: 200px;
        }

        .user-profile-img {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, #6c5ce7 0%, #a29bfe 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-weight: 700;
            font-size: 1.5rem;
            margin: 0 auto 0.75rem;
            border: 3px solid #fff;
            box-shadow: 0 4px 12px rgba(108, 92, 231, 0.3);
        }

        .user-profile h3 {
            font-size: 1rem;
            font-weight: 600;
            color: #2d3436;
            margin-bottom: 0.25rem;
        }

        .user-profile p {
            font-size: 0.85rem;
            color: #636e72;
            margin-bottom: 1rem;
        }

        .user-profile-actions {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }

        .user-profile-actions button {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            border: 1px solid #dfe6e9;
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .user-profile-actions button:hover {
            background: #6c5ce7;
            color: #fff;
            border-color: #6c5ce7;
        }

        /* Stats Cards */
        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stats-card {
            background: #fff;
            border-radius: 15px;
            padding: 1.75rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stats-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background: linear-gradient(180deg, #6c5ce7 0%, #a29bfe 100%);
        }

        .stats-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        }

        .stats-card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }

        .stats-card-icon {
            width: 48px;
            height: 48px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }

        .stats-card.users .stats-card-icon {
            background: rgba(108, 92, 231, 0.1);
            color: #6c5ce7;
        }

        .stats-card.payments .stats-card-icon {
            background: rgba(255, 107, 107, 0.1);
            color: #ff6b6b;
        }

        .stats-card.revenue .stats-card-icon {
            background: rgba(16, 172, 132, 0.1);
            color: #10ac84;
        }

        .stats-card h3 {
            font-size: 0.875rem;
            color: #636e72;
            font-weight: 500;
            margin-bottom: 0.5rem;
        }

        .stats-value {
            font-size: 2rem;
            font-weight: 700;
            color: #2d3436;
            margin-bottom: 0.5rem;
        }

        .stats-change {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .stats-change.positive {
            color: #10ac84;
        }

        .stats-change.negative {
            color: #ff6b6b;
        }

        .stats-meta {
            font-size: 0.8rem;
            color: #b2bec3;
        }

        /* Content Sections */
        .content-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .section-card {
            background: #fff;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #f5f6fa;
        }

        .section-title {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 1.25rem;
            font-weight: 700;
            color: #2d3436;
        }

        .section-title i {
            font-size: 1.5rem;
            color: #6c5ce7;
        }

        .section-meta {
            font-size: 0.875rem;
            color: #636e72;
            font-weight: 500;
        }

        /* Table Styles */
        .table-wrapper {
            overflow-x: auto;
            border-radius: 10px;
        }

        .data-table {
            width: 100%;
            border-collapse: collapse;
        }

        .data-table thead th {
            background: #f8f9fa;
            color: #636e72;
            font-weight: 600;
            padding: 1rem;
            text-align: left;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border-bottom: 2px solid #e9ecef;
        }

        .data-table tbody tr {
            border-bottom: 1px solid #f5f6fa;
            transition: background 0.2s ease;
        }

        .data-table tbody tr:hover {
            background: #f8f9fa;
        }

        .data-table tbody tr:last-child {
            border-bottom: none;
        }

        .data-table tbody td {
            color: #2d3436;
            padding: 1.25rem 1rem;
            vertical-align: middle;
        }

        /* Badges */
        .badge {
            padding: 0.4rem 0.9rem;
            border-radius: 8px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: capitalize;
            letter-spacing: 0.3px;
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
        }

        .badge-success {
            background: rgba(16, 172, 132, 0.1);
            color: #10ac84;
        }

        .badge-danger {
            background: rgba(255, 107, 107, 0.1);
            color: #ff6b6b;
        }

        .badge-warning {
            background: rgba(255, 159, 67, 0.1);
            color: #ff9f43;
        }

        .badge-info {
            background: rgba(108, 92, 231, 0.1);
            color: #6c5ce7;
        }

        .badge-primary {
            background: rgba(0, 123, 255, 0.1);
            color: #007bff;
        }

        /* User Elements */
        .user-avatar {
            width: 42px;
            height: 42px;
            border-radius: 12px;
            background: linear-gradient(135deg, #6c5ce7 0%, #a29bfe 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-weight: 700;
            font-size: 0.95rem;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-details h4 {
            color: #2d3436;
            font-size: 0.95rem;
            font-weight: 600;
            margin: 0 0 0.25rem 0;
        }

        .user-details p {
            color: #636e72;
            font-size: 0.8rem;
            margin: 0;
        }

        .amount {
            font-weight: 700;
            color: #10ac84;
            font-size: 1.05rem;
        }

        .tokens-badge {
            background: rgba(108, 92, 231, 0.1);
            color: #6c5ce7;
            padding: 0.35rem 0.85rem;
            border-radius: 8px;
            font-size: 0.85rem;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #636e72;
        }

        .empty-state i {
            font-size: 4rem;
            color: #dfe6e9;
            margin-bottom: 1rem;
        }

        .empty-state p {
            font-size: 1.1rem;
            color: #636e72;
            font-weight: 500;
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }

            .main-content {
                margin-left: 0;
                padding: 1.5rem 1rem;
            }

            .dashboard-header {
                flex-direction: column;
                align-items: flex-start;
            }

            .user-profile {
                width: 100%;
            }

            .stats-row {
                grid-template-columns: 1fr;
            }

            .section-card {
                padding: 1.25rem;
            }

            .data-table {
                font-size: 0.85rem;
            }

            .data-table thead th,
            .data-table tbody td {
                padding: 0.75rem 0.5rem;
            }
        }

        /* Scrollbar Styling */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #f5f6fa;
        }

        ::-webkit-scrollbar-thumb {
            background: #dfe6e9;
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #b2bec3;
        }
    </style>
</head>

<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="sidebar-logo">
            <h2><i class="bi bi-hexagon-fill"></i> NextAi</h2>
        </div>
        <ul class="sidebar-nav">
            <li><a href="/" class="active"><i class="bi bi-house-door-fill"></i> Home</a></li>
            <li><a href="/admin/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
            <li><a href=""><i class="bi bi-folder-fill"></i> Projects</a></li>
            <li><a href=""><i class="bi bi-people-fill"></i> Users</a></li>
            
        </ul>
        
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Dashboard Header -->
        <div class="dashboard-header">
            <div class="header-left">
                <h1>Hello, <%= user ? user.username : 'Admin' %></h1>
                <p>Track team progress here. You're doing great!</p>
            </div>
            <div class="header-right">
                <div class="date-badge">
                    <i class="bi bi-calendar3"></i>
                    <%= new Date().toLocaleDateString('en-US', { day: 'numeric', month: 'short', year: 'numeric' }) %>
                </div>
                <% if (user) { %>
                <div class="user-profile">
                    <div class="user-profile-img">
                        <%= user.username.charAt(0).toUpperCase() %>
                    </div>
                    <h3><%= user.username %></h3>
                    <p>@<%= user.username %></p>
                    <div class="user-profile-actions">
                        <button><i class="bi bi-telephone-fill"></i></button>
                        <button><i class="bi bi-chat-dots-fill"></i></button>
                        <button><i class="bi bi-three-dots-vertical"></i></button>
                    </div>
                </div>
                <% } %>
            </div>
        </div>

        <!-- Stats Cards -->
        <div class="stats-row">
            <div class="stats-card users">
                <div class="stats-card-header">
                    <div class="stats-card-icon">
                        <i class="bi bi-people-fill"></i>
                    </div>
                </div>
                <h3>Total Users</h3>
                <div class="stats-value"><%= users ? users.length : 0 %></div>
                <div>
                    <span class="stats-change positive">
                        <i class="bi bi-arrow-up"></i> +12%
                    </span>
                    <span class="stats-meta">vs last month</span>
                </div>
            </div>

            <div class="stats-card payments">
                <div class="stats-card-header">
                    <div class="stats-card-icon">
                        <i class="bi bi-credit-card-fill"></i>
                    </div>
                </div>
                <h3>Total Payments</h3>
                <div class="stats-value"><%= totalPayments || 0 %></div>
                <div>
                    <span class="stats-change positive">
                        <i class="bi bi-arrow-up"></i> +8%
                    </span>
                    <span class="stats-meta">successful transactions</span>
                </div>
            </div>

        </div>

        <!-- Users Section -->
        <div class="content-grid">
            <div class="section-card">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="bi bi-people"></i>
                        Registered Users
                    </h2>
                    <div class="section-meta">
                        Total: <%= users ? users.length : 0 %> users
                    </div>
                </div>
                <div class="table-wrapper">
                    <% if (users && users.length > 0) { %>
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>User</th>
                                <th>Username</th>
                                <th>Email</th>
                                <th>Role</th>
                                <th>Token Balance</th>
                                <th>Unlimited</th>
                                <th>Joined</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(user => { %>
                            <tr>
                                <td>
                                    <div class="user-info">
                                        <div class="user-avatar">
                                            <%= user.fullname.charAt(0).toUpperCase() %>
                                        </div>
                                        <div class="user-details">
                                            <h4><%= user.fullname %></h4>
                                        </div>
                                    </div>
                                </td>
                                <td><strong>@<%= user.username %></strong></td>
                                <td><%= user.email %></td>
                                <td>
                                    <% if (user.role === 'admin') { %>
                                    <span class="badge badge-warning">
                                        <i class="bi bi-shield-fill-check"></i> Admin
                                    </span>
                                    <% } else { %>
                                    <span class="badge badge-info">
                                        <i class="bi bi-person-fill"></i> User
                                    </span>
                                    <% } %>
                                </td>
                                <td>
                                    <span class="tokens-badge">
                                        <i class="bi bi-coin"></i> <%= user.tokenBalance %>
                                    </span>
                                </td>
                                <td>
                                    <% if (user.hasUnlimitedTokens) { %>
                                    <i class="bi bi-check-circle-fill" style="color: #10ac84; font-size: 1.25rem;"></i>
                                    <% } else { %>
                                    <i class="bi bi-x-circle-fill" style="color: #dfe6e9; font-size: 1.25rem;"></i>
                                    <% } %>
                                </td>
                                <td><%= new Date(user.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                    <% } else { %>
                    <div class="empty-state">
                        <i class="bi bi-inbox"></i>
                        <p>No users registered yet</p>
                    </div>
                    <% } %>
                </div>
            </div>

            <!-- Payments Section -->
            <div class="section-card">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="bi bi-credit-card-2-front"></i>
                        Payment Transactions
                    </h2>
                    <div class="section-meta">
                        Total: <%= payments ? payments.length : 0 %> transactions
                    </div>
                </div>
                <div class="table-wrapper">
                    <% if (payments && payments.length > 0) { %>
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>User</th>
                                <th>Plan</th>
                               
                                
                                <th>Currency</th>
                                <th>Tokens Added</th>
                                <th>Status</th>
                                <th>Provider</th>
                                <th>Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% payments.forEach(payment => { %>
                            <tr>
                                <td>
                                    <div class="user-info">
                                        <div class="user-avatar">
                                            <%= payment.user.fullname.charAt(0).toUpperCase() %>
                                        </div>
                                        <div class="user-details">
                                            <h4><%= payment.user.fullname %></h4>
                                            <p><%= payment.user.email %></p>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <span class="tokens-badge">
                                        <i class="bi bi-box"></i> <%= payment.planId %>
                                    </span>
                                </td>
                                
                                
                                <td><strong><%= payment.currency %></strong></td>
                                <td>
                                    <span class="tokens-badge">
                                        <i class="bi bi-coin"></i> <%= payment.tokensAdded %>
                                    </span>
                                </td>
                                <td>
                                    <% if (payment.status === 'paid') { %>
                                    <span class="badge badge-success">
                                        <i class="bi bi-check-circle-fill"></i> Success
                                    </span>
                                    <% } else if (payment.status === 'failed') { %>
                                    <span class="badge badge-danger">
                                        <i class="bi bi-x-circle-fill"></i> Failed
                                    </span>
                                    <% } else { %>
                                    <span class="badge badge-warning">
                                        <i class="bi bi-clock-fill"></i> Pending
                                    </span>
                                    <% } %>
                                </td>
                                <td>
                                    <span class="badge badge-primary">
                                        <i class="bi bi-credit-card"></i> <%= payment.provider %>
                                    </span>
                                </td>
                                <td>
                                    <% if (payment.paidAt) { %>
                                    <%= new Date(payment.paidAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                                    <br>
                                    <small style="color: #b2bec3;">
                                        <%= new Date(payment.paidAt).toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' }) %>
                                    </small>
                                    <% } else { %>
                                    <%= new Date(payment.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                                    <% } %>
                                </td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                    <% } else { %>
                    <div class="empty-state">
                        <i class="bi bi-inbox"></i>
                        <p>No payment transactions yet</p>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>

    </div>

    <script src="/assets/js/notifications.js"></script>
    <script>
        const errorMessage = <%- JSON.stringify(errorMessage || null) %>;
        const successMessage = <%- JSON.stringify(successMessage || null) %>;

        if (errorMessage) {
            notify.error(errorMessage);
        }

        if (successMessage) {
            notify.success(successMessage);
        }
    </script>
    <script src="/assets/js/bootstrap.bundle.js"></script>
</body>

</html>
